[package]
name = "auth"
version = "0.1.0"
edition = "2021"

[dependencies]
chrono = { version = "0.4", features = ["serde"] }
domain = { path = "../domain" }
jsonwebtoken = "9"
serde = { version = "1", features = ["derive"] }
uuid = { version = "1", features = ["v4"] }
